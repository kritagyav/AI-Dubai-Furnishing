// Stylelint configuration for enforcing design token usage
// Ensures visual identity values come from Style Dictionary tokens, not raw CSS
// Full implementation in Story 1.4 with design token pipeline

/** @type {import('stylelint').Config} */
export default {
  rules: {},
};
